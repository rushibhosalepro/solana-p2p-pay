{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "turbo run build --filter=fe",
  "builds": [
    {
      "src": "apps/fe/package.json",
      "use": "@vercel/next",
      "config": { "distDir": "apps/fe/.next" }
    }
  ]
}
